# Thesis dashboard (web)

Single app lives in **`dashboard/`** (Vite + React + TypeScript + Tailwind).

```bash
cd dashboard
npm install
npm run dev
```

Deploy (e.g. Vercel): set project root to **`dashboard`**. See `dashboard/README.md` and `TODO.md`.
